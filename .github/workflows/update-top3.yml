name: Update TOP-3 data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Автообновление временно отключено. Не запускать до установки проверенного парсера Столото."
