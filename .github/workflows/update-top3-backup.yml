name: TOP-3 backup updater

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Резервное автообновление временно отключено. Не запускать до установки проверенного парсера Столото."
